import React from 'react';
import { View, Text } from 'react-native';

export const TutorialCarousel = () => {
  return (
    <View style={{flex:1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>El tutorial no es compatible con la web</Text>
    </View>
  )
}